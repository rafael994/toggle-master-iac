apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: evaluation-hpa
  namespace: evaluation # Altere para o namespace correto, se necessário
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: evaluation # Nome do deployment do seu service
  minReplicas: 2     # Número mínimo de pods
  maxReplicas: 10    # Número máximo de pods
  targetCPUUtilizationPercentage: 70